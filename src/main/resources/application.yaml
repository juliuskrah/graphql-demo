spring:
  threads:
    virtual:
      enabled: true
  graphql:
    graphiql:
      enabled: true
  messages:
    basename: messages/messages
    encoding: UTF-8
  docker:
    compose:
      file: src/main/resources/compose.yml
  data:
    mongodb:
      database: test

mongock:
  enabled: true
#  migration-scan-package: com.example.graph.migration
  runner-type: applicationrunner
  transaction-enabled: true
