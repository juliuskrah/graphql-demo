query productNodeDetails($id: ID!) {
    node(id: $id) {
        id
        __typename
        ... on Product {
            title
            description
            createdAt
            updatedAt
        }
    }
}
